services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: citadelle
      POSTGRES_PASSWORD: citadelle
      POSTGRES_DB: citadelle
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U citadelle"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
