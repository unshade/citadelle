# Required - JWT signing secret (generate a strong one for production)
JWT_SECRET=change-me-in-production-min-32-characters-long

# Optional - Server port (default: 8080)
# PORT=8080

# Optional - Database path (default: citadelle.db)
# DB_PATH=citadelle.db
